name: Language Feed Test
on:
  push:
    branches: [main]
    paths:
      - 'language-feed-model.js'
      - 'language-four-feed-model.js'
      - 'languages-feed.js'
      - 'languages-feed.css'
      - 'languages.html'
      - 'language-games.html'
      - 'language-games.js'
      - 'language-games.css'
      - 'tests/language-feed.test.js'
      - '.github/workflows/language-feed-test.yml'
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check feed and games JavaScript syntax
        run: |
          node --check language-feed-model.js
          node --check language-four-feed-model.js
          node --check languages-feed.js
          node --check language-games.js
      - name: Run feed and games behavior tests
        run: node tests/language-feed.test.js
