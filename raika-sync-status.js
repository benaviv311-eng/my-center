async function rssRefresh(){if(!window.RaikaPrivate?.authorized||!window.RaikaWorkspaceClient)return;let rows=[];try{rows=(await RaikaWorkspaceClient.call({action:'list'})).items||[];}catch{return;}const map=new Map(rows.map(r=>[`${r.item_type}:${r.item_id}`,r]));document.querySelectorAll('#ideas-grid .writer-card').forEach(card=>{const bar=card.querySelector('.private-card-actions');if(!bar)return;bar.querySelector('.raika-sync-state')?.remove();const row=map.get(`${card.dataset.rpType}:${card.dataset.rpId}`);if(!row||!['canon','archived'].includes(row.status))return;const label=row.sheet_sync_status==='synced'?'☁️ שיטס מסונכרן':row.sheet_sync_status==='error'?'⚠️ סנכרון נכשל':'⏳ ממתין לשיטס';bar.insertAdjacentHTML('beforeend',`<span class="raika-sync-state meta">${label}</span>`);});}
document.addEventListener('raika:private-ready',()=>setTimeout(rssRefresh,80));document.addEventListener('raika:private-rendered',()=>setTimeout(rssRefresh,80));window.RaikaSyncStatus={refresh:rssRefresh};
